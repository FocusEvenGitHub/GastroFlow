{
  "name": "gastroflow-e2e",
  "private": true,
  "description": "Testes de navegador do GastroFlow (spec 040). Isolados aqui de propósito: o projeto não tem build step e nada em public/ depende de Node.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
